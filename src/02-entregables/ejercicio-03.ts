console.log("************** DELIVERABLE 03 *********************");

const source = {
    name: "Nombre",
    surname: "Apellido",
    age: 32
}

const target ={
    age:32,
    
}

const clone = (source) => {
    const result = source[name]
    return result;
}

console.log(clone(source))
source.name = "paxo";
